# === DataSource for recommendations ===
# Укажите путь к вашему исходному H2-файлу transaction (БЕЗ .mv.db).
# По умолчанию ожидаем файл рядом с jar: ./transaction.mv.db
recommendations.db.url=${RECOMMENDATIONS_DB_URL:jdbc:h2:file:./transaction;AUTO_SERVER=TRUE}
recommendations.db.username=${RECOMMENDATIONS_DB_USERNAME:sa}
recommendations.db.password=${RECOMMENDATIONS_DB_PASSWORD:}

# logging.level.org.springframework.jdbc.core=DEBUG
